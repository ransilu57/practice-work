let fruits = ["apple", "orange", "banana", "coconut"];

/*fruits.push("coconut"); //adds a new element to the end of an array
fruits.pop(); //removes the last element

fruits.unshift("mango")//adds a new elemnt to the begining of an array
fruits.shift();//removes an element from the begining

let numOfFruits = fruits.length; //length of the array
let index = fruits.indexOf("banana");//index of an element

console.log(index);
console.log(numOfFruits);
console.log(fruits[0]); */

//fruits.sort(); // sorts the elements in alphabetical order
fruits.sort().reverse(); // reverses the above task





// a shortcut to display the elements of an array

for(let fruit of fruits){
    console.log(fruit);
}
